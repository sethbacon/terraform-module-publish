{
  "name": "terraform-module-publish",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: publish a Terraform/OpenTofu module version to a self-hosted registry or HCP Terraform / TFE.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify --no-source-map-register",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@4cloudguru/pipeline-task-core": "0.2.0",
    "@actions/core": "^1.11.1",
    "undici": "^6.28.0"
  },
  "overrides": {
    "undici": "^6.28.0"
  },
  "devDependencies": {
    "@types/node": "^26",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
